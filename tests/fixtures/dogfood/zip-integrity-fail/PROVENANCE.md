FIXTURE PROVENANCE: synthetic fixture — zip-integrity, constructed from matrix §2 MUST_BLOCK spec (no real product occurrence as of D137). `extension.zip` built via a minimal STORE-method zip writer (see `/tmp/mkzip.mjs` used at authoring time) with a deliberately duplicated `background.js` central-directory entry, same defect class as the release zip incident referenced in commit `5d8b775`.
