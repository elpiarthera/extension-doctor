{
  "manifest_version": 3,
  "name": "synthetic fixture — manifest-permission-allowlist, constructed from matrix §2 MUST_PASS spec (no real product occurrence as of D137)",
  "version": "1.0.0",
  "permissions": ["storage", "scripting", "alarms"]
}
