// FIXTURE PROVENANCE: synthetic barrel file, labeled synthetic —
// declared as an intentional stable public API barrel via
// .extension-doctor.json allowedBarrels.
export { Button } from "./button.js";
