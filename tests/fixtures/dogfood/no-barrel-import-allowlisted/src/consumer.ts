// FIXTURE PROVENANCE: synthetic consumer, labeled synthetic —
// imports the allowlisted barrel intentionally.
import { Button } from "./components/index.js";

export function render(): string {
  return Button();
}
