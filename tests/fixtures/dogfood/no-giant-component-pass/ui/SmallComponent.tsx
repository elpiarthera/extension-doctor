// FIXTURE PROVENANCE: synthetic 120-line component, labeled synthetic (not extracted from real history).
import type { JSX } from "preact";

export interface SmallProps {
  value: number;
}

export function Small({ value }: SmallProps): JSX.Element {
  return <div>{value}</div>;
}
