// FIXTURE PROVENANCE: synthetic, labeled synthetic — no manifest.json or
// vite.config present anywhere under this fixture root, so buildExportGraph
// cannot resolve any entry point.
export function orphan(): string {
  return "orphan";
}
