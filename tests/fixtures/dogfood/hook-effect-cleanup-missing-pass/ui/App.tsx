// FIXTURE PROVENANCE: synthetic, labeled — companion regression fixture,
// same string-literal-only trigger shape as ui/panel.tsx in this fixture
// set, placed in a different file to prove the fix generalizes across
// files, not just one lucky match.
export const CODE_SAMPLE =
  "useEffect(() => { window.addEventListener(\"resize\", r); }, [])";
