# fixture: no src/content, src/ui or ui directory present

This directory intentionally contains no scannable source root so that
shadow-dom-style-leak reports "inconclusive" rather than a silent pass.
