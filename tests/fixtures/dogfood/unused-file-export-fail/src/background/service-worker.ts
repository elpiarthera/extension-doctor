// FIXTURE PROVENANCE: synthetic entry point, labeled synthetic (generalized
// from react-doctor dead-barrel audit pattern, docs/analysis §1.4 §2 rule 5).
import { used } from "../core/used";

export function boot(): string {
  return used();
}
