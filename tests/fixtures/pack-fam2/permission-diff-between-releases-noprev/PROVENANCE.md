# FIXTURE PROVENANCE

Synthetic fixture. `manifest.json` is present but no
`.extension-doctor/prev-manifest.json` snapshot exists — the rule cannot
compute a diff and MUST report INCONCLUSIVE, never "pass".
