# FIXTURE PROVENANCE

Synthetic fixture (not extracted from git history). Current `manifest.json`
gains `"cookies"` in `permissions[]` versus
`.extension-doctor/prev-manifest.json`, but `CHANGELOG.md` documents the
escalation explicitly.
