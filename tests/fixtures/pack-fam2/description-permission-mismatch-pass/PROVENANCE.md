# FIXTURE PROVENANCE

`manifest.json` in this directory is a verbatim copy of
`gptpowerups-extension` current manifest.json at commit `b2805d2` (fix "remove
Cursor mismatch (B-2), move tabs to optional_permissions (M-1)"), where
`"tabs"` lives in `optional_permissions[]`, not `permissions[]`.
