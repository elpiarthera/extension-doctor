/**
 * MUST_BLOCK fixture — a hardcoded host DOM selector literal with no
 * `// verified:` comment pointing to a dated DOM fixture.
 */
export const SITE_CONFIG = {
  composerSelector: document.querySelector('[data-testid="composer-input"]'),
};
