extension-doctor — prior art notice

This project is written from scratch. No source code is copied, ported, or
derived from any of the projects below. Each is cited because its *ideas*
were studied while designing the rule pack — studied as prior art, no code
derived.

1. dot-skills
   https://github.com/pproenca/dot-skills
   License: MIT (Copyright (c) 2025)
   Relationship: studied as prior art, no code derived. The general idea
   documented in its "avoid broadcast to all tabs" and "handle context
   invalidated" notes informed the intent of the `net-broadcast-unfiltered`
   and `sw-context-invalidated-guard` rules. No dot-skills file, function,
   or text is included in this repository.

2. addons-linter
   https://github.com/mozilla/addons-linter
   License: MPL-2.0
   Relationship: studied as prior art, no code derived. MPL-2.0 is
   copyleft per-file; consulting it as an example of a mature
   WebExtension-linter shape does not require licensing this project
   under MPL-2.0, because no addons-linter file or code fragment is
   present here.

3. react-doctor
   https://www.npmjs.com/package/react-doctor
   License: "Modified MIT" (SEE LICENSE IN LICENSE on npm; not plain MIT).
   Restricts, without prior written authorization from Million Software,
   Inc.: (a) using the Software, its source code, or any derivative works
   thereof, in whole or in part, as training, fine-tuning, or evaluation
   data, or as input to any automated pipeline for training or improving
   any machine learning model or AI system; and (b) selling the Software,
   or offering it to third parties as a paid, hosted, or managed product
   or service (including any commercial API or SaaS offering) whose value
   derives entirely or substantially from the Software.
   Relationship: studied as prior art, no code derived. react-doctor was
   run as a published CLI tool (`npx react-doctor`) against this project's
   own codebase, as an end user would. Its source was never read, copied,
   or ported, and it was never used as training, fine-tuning, or
   evaluation data, nor as input to any ML/AI training pipeline — this
   project's use conforms to both restrictions (a) and (b) above. Its
   restrictive license governs react-doctor itself, not extension-doctor.

   VERSION PIN COROLLARY: any invocation of react-doctor in this project's
   harness (or the wider fleet) MUST pin `react-doctor@0.2.11`, never
   `latest`. 0.2.11 ships under plain MIT — commercial use is free. Later
   releases moved to the Modified MIT above, which requires Million
   Software's prior written authorization for commercial use; verify any
   target version with `npm view react-doctor@<version> license` (a bare
   `MIT` is safe, `SEE LICENSE IN LICENSE` is the Modified MIT and is not)
   before bumping. The pin is therefore not a harness-stability
   precaution — it is the only thing keeping any commercial run of
   react-doctor against this (commercially-licensed) codebase on the
   permissive side of their license. Do not bump past 0.2.11 without first
   re-reading the target version's LICENSE on npm.

extension-doctor's own license is documented in ./LICENSE.
